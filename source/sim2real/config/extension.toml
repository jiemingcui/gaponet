[package]

# Semantic Versioning is used: https://semver.org/
version = "0.1.0"

# Description
category = "isaaclab"
readme  = "README.md"

title = "GapONet Extension"
author = "GapONet Developers"
maintainer = "GapONet Developers"
description = "GapONet: Sim-to-Real Humanoid Robot Control with DeepONet, Transformer, and MLP architectures"
repository = "https://github.com/yourusername/gaponet"
keywords = ["gaponet", "sim2real", "humanoid", "deeponet", "transformer", "isaaclab", "robotics"]

[dependencies]
"isaaclab" = {}
"isaaclab_assets" = {}
"isaaclab_mimic" = {}
"isaaclab_rl" = {}
"isaaclab_tasks" = {}
# NOTE: Add additional dependencies here

[[python.module]]
name = "sim2real"

[isaaclab_settings]
# TODO: Uncomment and list any apt dependencies here.
#       If none, leave it commented out.
# apt_deps = ["example_package"]
# TODO: Uncomment and provide path to a ros_ws
#       with rosdeps to be installed. If none,
#       leave it commented out.
# ros_ws = "path/from/extension_root/to/ros_ws"
