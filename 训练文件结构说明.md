# GapONet 训练文件结构说明

本文档整理了训练相关的所有主要文件，包括训练代码、数据、pipeline和设计文件。

## 📁 1. 训练脚本（Training Scripts）

### 主要训练入口
- **`scripts/rsl_rl/train.py`** - RSL-RL训练主脚本（当前使用）
- **`scripts/reinforcement_learning/rsl_rl/train.py`** - 标准RSL-RL训练脚本
- **`scripts/reinforcement_learning/rl_games/train.py`** - RL Games训练脚本
- **`scripts/reinforcement_learning/sb3/train.py`** - Stable Baselines3训练脚本
- **`scripts/reinforcement_learning/skrl/train.py`** - SKRL训练脚本

### 评估/播放脚本
- **`scripts/rsl_rl/play.py`** - 模型评估/播放脚本
- **`scripts/rsl_rl/deploy.py`** - 轻量级推理脚本（无需Isaac Sim）
- **`scripts/rsl_rl/inference_jit.py`** - JIT模型导出脚本

### 辅助脚本
- **`scripts/rsl_rl/cli_args.py`** - 命令行参数处理
- **`scripts/rsl_rl/process_env.py`** - 环境处理工具（处理letter参数等）
- **`scripts/rsl_rl/plot_delta_action.py`** - 动作可视化脚本

## 📁 2. 训练Pipeline（Training Pipeline）

### Runner（训练执行器）
- **`source/sim2real/sim2real/rsl_rl/runners/operator_runner.py`** - 主要训练Runner（DeepONet）
  - 实现OperatorRunner类
  - 处理模型学习、传感器数据、环境采样
  - 包含CSV日志写入逻辑
  
- **`source/sim2real/sim2real/rsl_rl/runners/operator_vanilla_runner.py`** - 简化版Runner
- **`source/sim2real/sim2real/rsl_rl/runners/base_runner.py`** - Runner基类

### 训练流程
```
train.py 
  → 创建环境 (gym.make)
  → 创建Runner (OperatorRunner)
  → runner.learn() 
    → 数据收集 (collect_rollouts)
    → 策略更新 (alg.update)
    → 日志记录 (log)
```

## 📁 3. 环境（Environments）

### 主要环境实现
- **`source/sim2real/sim2real/tasks/humanoid_operator/humanoid_operator_env_fourior.py`** 
  - Fourior机器人环境实现
  - 包含reward计算、观测构建、动作应用
  - 包含CSV日志记录功能
  
- **`source/sim2real/sim2real/tasks/humanoid_operator/humanoid_operator_env.py`**
  - 原始Humanoid Operator环境

- **`source/sim2real/sim2real/tasks/humanoid_operator/humanoid_operator_env_cfg_fourior.py`**
  - Fourior环境配置（episode长度、传感器配置等）

- **`source/sim2real/sim2real/tasks/humanoid_operator/humanoid_operator_env_cfg.py`**
  - 原始环境配置

### 环境注册
- **`source/sim2real/sim2real/tasks/humanoid_operator/__init__.py`**
  - 使用gym.register注册环境
  - 注册了多个变体（Delta-Action, Fourior, Vanilla等）

### 辅助工具
- **`source/sim2real/sim2real/tasks/humanoid_operator/operator_helper.py`**
  - 传感器位置设置、质量设置等工具函数

## 📁 4. 数据（Data）

### 数据加载器
- **`source/sim2real/sim2real/tasks/humanoid_operator/motions/motion_motor_loader.py`**
  - MotionLoaderMotor类
  - 加载.npz格式的动作数据
  - 提供采样、索引管理功能

### 数据文件
- **`source/sim2real/sim2real/tasks/humanoid_operator/motions/motion_amass/fourior/merged_50Hz_31_10_payload.npz`**
  - 训练数据文件（432个motion片段，每个31个关节）
  
- **`source/sim2real/sim2real/motions/motions_amass/edited_27dof/test.npz`**
  - 测试数据文件

### 数据处理脚本
- **`process_real_data.py`** - 真实数据处理脚本
- **`process_real_data说明.md`** - 数据处理说明文档

### 关节名称映射
- **`source/sim2real/sim2real/tasks/humanoid_operator/motions/joint_names.py`**
  - 关节名称字典（ROBOT_JOINT_NAME_DICT_URDF_FOURIOR等）

## 📁 5. 网络架构（Network Architectures）

### 主要网络模块
- **`source/sim2real/sim2real/rsl_rl/modules/deeponet_actor_critic.py`**
  - DeepONetActorCritic类
  - 实现DeepONet架构的Actor-Critic网络
  - 包含Branch网络、Trunk网络、Critic网络、Model网络

- **`source/sim2real/sim2real/rsl_rl/modules/ActorCriticTransformer.py`**
  - Transformer架构的Actor-Critic网络

### 网络组件
- **`source/sim2real/sim2real/rsl_rl/networks/multi_res_branch_net.py`**
  - MultiResolutionBranchNet类
  - 多分辨率分支网络（用于DeepONet）

- **`source/sim2real/sim2real/rsl_rl/networks/lnn_sensor_model.py`**
  - 传感器模型网络

### 模块导出
- **`source/sim2real/sim2real/rsl_rl/modules/__init__.py`**
  - 导出DeepONetActorCritic和ActorCriticTransformer

## 📁 6. 配置（Configurations）

### Agent配置
- **`source/sim2real/sim2real/tasks/humanoid_operator/agents/rsl_rl_operator_cfg.py`**
  - HumanoidOperatorRunnerCfg配置类
  - DeepONetActorCriticCfg配置
  - PPO算法参数配置
  - 实验名称、日志配置等

- **`source/sim2real/sim2real/tasks/humanoid_operator/agents/rsl_rl_ppo_cfg.py`**
  - 标准PPO配置

### 环境配置
- **`source/sim2real/sim2real/tasks/humanoid_operator/humanoid_operator_env_cfg_fourior.py`**
  - Fourior环境配置
  - 包含传感器配置、模型历史配置等

## 📁 7. 机器人资源（Robot Assets）

### 机器人配置
- **`source/sim2real_assets/sim2real_assets/robots/fourior.py`**
  - Fourior机器人配置（ArticulationCfg）
  - 执行器配置、关节限制等

- **`source/sim2real_assets/sim2real_assets/robots/unitree_h1_2.py`**
  - Unitree H1机器人配置

### 资源文件位置
- **URDF文件**: `source/sim2real_assets/sim2real_assets/urdfs/`
- **USD文件**: `source/sim2real_assets/sim2real_assets/usds/`

## 📁 8. 训练Pipeline详细流程

### 训练启动流程
```
1. scripts/rsl_rl/train.py
   ├─ 解析命令行参数
   ├─ 启动Isaac Sim (AppLauncher)
   └─ 调用main()函数

2. main()函数
   ├─ 加载环境配置 (env_cfg)
   ├─ 加载Agent配置 (agent_cfg)
   ├─ 创建环境 (gym.make)
   ├─ 创建Runner (OperatorRunner)
   └─ 开始训练 (runner.learn())
```

### Runner训练循环
```
OperatorRunner.learn()
├─ 初始化策略网络 (DeepONetActorCritic)
├─ 初始化算法 (PPO)
└─ 训练循环 (for each iteration):
   ├─ 数据收集阶段
   │  ├─ env.step() → 环境步进
   │  ├─ 收集观测、动作、奖励
   │  └─ 累积到缓冲区
   ├─ 策略更新阶段
   │  ├─ 计算returns
   │  ├─ 更新Actor网络
   │  └─ 更新Critic网络
   └─ 日志记录阶段
      ├─ 记录loss、reward等
      ├─ 输出到terminal
      └─ 写入CSV（per-joint rewards）
```

### 环境步进流程
```
HumanoidOperatorEnv.step()
├─ _pre_physics_step() → 接收动作
├─ _apply_action() → 应用动作到机器人
├─ _raw_step_simulator() → 物理仿真步进
├─ _get_observations() → 获取观测
├─ _get_rewards() → 计算奖励
│  └─ _reward_tracking() → 计算跟踪误差
├─ _get_dones() → 检查episode是否结束
└─ step_operator() → Operator特定逻辑
   ├─ 更新time_indices
   ├─ 计算per-joint rewards
   └─ 返回info（包含reward_per_joint）
```

## 📁 9. 关键设计文件

### 核心设计
- **README.md** - 项目总览和使用说明
- **setup.sh** - 环境安装脚本
- **Fourior任务配置说明.md** - Fourior任务配置说明

### 数据处理
- **process_real_data.py** - 真实数据处理
- **process_real_data说明.md** - 数据处理文档
- **merge_robot_npz.py** - 合并机器人npz数据

## 📁 10. 日志和输出

### 训练日志
- **logs/rsl_rl/** - 训练日志目录
  - 包含模型checkpoint、配置文件、tensorboard日志

### CSV日志
- **source/sim2real/sim2real/tasks/humanoid_operator/logs/per_joint_rewards/**
  - 每个关节的reward记录（按iteration）

### WandB日志
- **wandb/** - WandB实验跟踪日志

## 📊 数据流图

```
训练数据 (merged_50Hz_31_10_payload.npz)
  ↓
MotionLoaderMotor (加载和采样)
  ↓
HumanoidOperatorEnv (环境)
  ├─ 观测构建 (compute_operator_observation)
  ├─ 奖励计算 (_get_rewards)
  └─ Episode管理
  ↓
OperatorRunner (训练执行)
  ├─ 数据收集
  ├─ 策略更新 (PPO)
  └─ 日志记录
  ↓
DeepONetActorCritic (网络)
  ├─ Branch Network (处理传感器数据)
  ├─ Trunk Network (处理动作目标)
  ├─ Critic Network (价值估计)
  └─ Model Network (传感器模型)
```

## 🔑 关键参数说明

### 训练参数（从train.py）
- `--task`: 任务名称（如Isaac-Humanoid-Operator-Delta-Action-Fourior）
- `--num_envs`: 并行环境数量（默认4080）
- `--max_iterations`: 最大训练迭代次数
- `--experiment_name`: 实验名称
- `--letter`: 关节/数据选择（amass表示使用AMASS数据）
- `--run_name`: 运行名称
- `--device`: 计算设备（cuda/cpu）

### 环境参数（从env_cfg）
- `episode_length_s`: Episode长度（秒）
- `decimation`: 动作执行频率（每N个物理步执行一次）
- `num_sensor_positions`: 传感器位置数量（20）
- `action_space`: 动作空间维度（31个关节）

### 网络参数（从agent_cfg）
- `branch_input_dims`: Branch网络输入维度列表
- `trunk_input_dim`: Trunk网络输入维度
- `critic_input_dim`: Critic网络输入维度
- `model_input_dim`: Model网络输入维度
- `hidden_dims`: 隐藏层维度

## 📝 总结

训练系统的核心组件：
1. **训练脚本**: `scripts/rsl_rl/train.py`
2. **环境**: `humanoid_operator_env_fourior.py`
3. **Runner**: `operator_runner.py`
4. **网络**: `deeponet_actor_critic.py`
5. **数据**: `motion_motor_loader.py` + `.npz`文件
6. **配置**: `rsl_rl_operator_cfg.py` + `humanoid_operator_env_cfg_fourior.py`

